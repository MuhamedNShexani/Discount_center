/**
 * Full-screen vertical reels viewer (TikTok / Facebook Reels style).
 * Implemented in `pages/reels.js` — re-exported here for the `components/reels/` module.
 */
export { default } from "../../pages/reels";
